# A1R.4 / Ш0 — пробы П1 и П2

Артефакты: `spikes/A1R.4/` (`p1.mjs`, `run-p1.sh`, `summarize-p1.sh`, `p2.mjs`, `run-p2.sh`,
сырые выводы в `out/`). Стенд изолирован: свой `HOME`, свой `PI_CODING_AGENT_DIR`,
faux-провайдер на localhost, `PI_OFFLINE=1`, фикстурный stdio-MCP-сервер без сети.
Хост-обвязка — `spikes/A1R.0/c3c4/host.mjs` (повторяет `open()` upstream, окно `bind`).

## П1. MCP и cwd — исход **B**, гейт Д10 остаётся

Замер: проектный конфиг с двумя фикстурными серверами лежит у cwd листа (Y),
родительский `process.cwd()` = X (в X проектного конфига нет — проверяется в самой пробе).
Запрошено `MCP_DIRECT_TOOLS=fx/alpha,fx/gamma,fy/beta,fy/delta`; «полон» = все четыре
имени в `session.getActiveToolNames()`.

| прогон | cwd | слой | `--mcp-config` | полон на возврате `create()` | полон через 2.5 с | полон после prompt |
|---|---|---|---|---|---|---|
| `v2261-p1-match` | X = Y | `.pi/mcp.json` | нет | **да** | да | да |
| `v2261-p1-mismatch` | X ≠ Y | `.pi/mcp.json` | нет | **нет** | да | да |
| `v2261-p1-mismatch-mcp` | X ≠ Y | `.mcp.json` | нет | **нет** | да | да |
| `v2261-p1-mismatch-warm` | X ≠ Y | `.pi/mcp.json` | нет (кэш прогрет) | **нет** | да | да |
| `v2261-p1-mismatch-ovr` | X ≠ Y | `.pi/mcp.json` | да | **да** | да | да |
| `v2340-*` | те же пять | | | те же пять исходов | | |

Вывод: при `process.cwd() ≠ ctx.cwd` набор direct-инструментов на момент возврата
`create()` — то есть ровно в точке снимка реестра (С2) и установки барьера (С1) —
**неполон**; недостающие имена доезжают асинхронно позже. Положительный контроль
(`match`) на том же стенде полон уже на возврате. Значит И6 «ровно десять `bsl-*`»
при расхождении cwd недостижим, и закрытый отказ Д10 обязателен.

Механика: при пустом раннем конфиге (`loadMcpConfig(earlyConfigPath)` без cwd,
`config.ts:293` → `process.cwd()`) адаптер не знает, каких серверов ждать, и обработчик
`session_start` не дожидается инициализации (`index.ts:407-415`); при совпадении cwd —
дожидается.

Исход **C** тоже показан: явная передача пути конфигурации (`--mcp-config`, она же
`options.configPath`, `index.ts:116-121`) делает набор полным уже на возврате `create()`.
Это рабочая альтернатива Д10 и предмет вилки **В1** для человека; по умолчанию
остаётся закрытый отказ.

Побочный факт для A1R.6: в адаптере 2.34.0 `PI_MCP_CONFIG_MODE=exclusive` схлопывает
источники до одного глобального (`config.ts:463-472`) и **выключает проектные слои**
целиком; в 2.26.1 такой переменной нет. Первый прогон 2.34.0 провалил положительный
контроль именно из-за неё, а не из-за cwd. Если onecpi когда-либо выставит эту
переменную, проектный `.pi/mcp.json` листа перестанет читаться.

## П2. Самопроверка сессии — Д9 подтверждена, В2 закрыта веткой по умолчанию

Одноразовая проба: `SessionManager.inMemory`, без `bindExtensions` и без `prompt`,
затем `dispose()`.

| прогон | вердикт | обращений к провайдеру | ФС до = после | мс |
|---|---|---|---|---|
| `p2-real` | true | 0 | нет (+2) | 15.3 |
| `p2-real-repeat` | true | 0 | нет (+2) | 13.8 |
| `p2-warm` (прогретый agent-dir) | true | 0 | **да** | 14.9 |
| `p2-warm-again` (тот же каталог повторно) | true | 0 | **да** | 16.1 |
| `p2-fake-no-stream` (контроль) | **false** | 0 | нет (+2) | 14.4 |

Прирост на холодном каталоге — ровно `auth.json` и `models-store.json` (по 2 байта,
`{}`), их пишет `ModelRuntime.create()`; в рабочем каталоге сессии (`ws/`) не появляется
ничего, сессионного файла нет. На прогретом каталоге — форма, в которой самопроверка и
идёт в производстве (хост уже создал `ModelRuntime`), — дрейфа ФС нет вовсе.

Проверено на настоящем Pi 0.85.1: `session.agent`, `agent.streamFunction` (функция и
затеняется собственным свойством), `session.getActiveToolNames` (вернул 4 встроенных),
`"loaded" in loader`. Отрицательный контроль — модуль, чей `Agent` лишён
`streamFunction`, — даёт `verdict: false` (упал на `getActiveToolNames`, читающем
`agent.tools`), то есть проба действительно проваливаема.

Ш9 обязан снимать все четыре поля (пункт 4 чек-листа исполнителя A1R.4).
